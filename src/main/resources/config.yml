auto-join:
  enabled: true    # If this is enabled, players that join the (server or world depending on scope) will automatically be added to an SSG game.
  scope: server    # Scope
                   #   server: Any player who joins the server will be automatically added
                   #   world: Any player who joins the world will be automatically added


game:

  # Send game info to a discord webhook
  webhook:
    enabled: true
    url: "YOUR WEBHOOK URL HERE"

    log-events:
      - lobby_join
      - lobby_leave
      - game_join
      - game_leave
      - death
      - win_recap
      - chat

  respawns:
    # Number of lives each player starts with. A player's life is removed if they are eliminated.
    default-lives: 1

    # Time (in seconds) a player must wait before respawning. This only applies if they still have remaining lives.
    respawn-seconds: 5

  misc:
    # The format of player names that appear in the tab list
    player-tab-name-format: "{player-rank-color}{player-team-prefix}{player-display-name}"

    # Should players keep their inventory upon a non-final death. (Players will always drop their inventory upon final death.)
    keep-inventory: false

    # Should players be allowed to join an ongoing countdown after everyone has already been placed into their starting cages?
    allow-join-during-start: true

    # Default gamemode used for active players (spectators excluded).
    default-gamemode: adventure

  click-actions:
    # Execute commands upon right or left-clicking with an item in the hand.
    #    item: a Minecraft item
    #    click-type: [RIGHT_CLICK, LEFT_CLICK]
    #    actions: Command actions executed from the console.

    gray-dye:
      click-type: RIGHT_CLICK
      actions:
        - "minecraft:item replace entity {player} hotbar.4 with minecraft:lime_dye[minecraft:custom_name=[{\"text\":\"Ready\",\"italic\":false,\"color\":\"green\"}],lore=[[{\"text\":\"Right click to toggle your ready status.\",\"italic\":false,\"color\":\"yellow\"}]]] 1"
        - sudo {player} -s ssg ready

    lime-dye:
      click-type: RIGHT_CLICK
      actions:
        - sudo {player} -s ssg ready
        - "minecraft:item replace entity {player} hotbar.4 with minecraft:gray_dye[minecraft:custom_name=[{\"text\":\"Not Ready\",\"italic\":false,\"color\":\"red\"}],lore=[[{\"text\":\"Right click to toggle your ready status.\",\"italic\":false,\"color\":\"white\"}]]] 1"



  command-actions:
    # Execute console commands upon common game events.
    # Leaving a list empty will execute no commands.
    # Commands should NOT begin with "/"

    on-lobby:
      # Available placeholders: {player}, {game}
      # NOT A LOOP! Runs for individual players in the lobby if they newly join.
      - "gamerule send_command_feedback false"
      - "clear {player}"
      - "minecraft:xp set {player} 0 levels"
      - "minecraft:xp set {player} 0 points"
      - "minecraft:item replace entity {player} hotbar.4 with minecraft:gray_dye[minecraft:custom_name=[{\"text\":\"Not Ready\",\"italic\":false,\"color\":\"red\"}],lore=[[{\"text\":\"Right click to toggle your ready status. If all players are ready, the lobby countdown timer will be skipped.\",\"italic\":false,\"color\":\"white\"}]]] 1"


    on-prep:
      # Available placeholders: {game}
      # NOT A LOOP! Runs ONLY one time while the arena is prepped and players are spawned.
      - "difficulty hard"
      - "gamerule locator_bar false"
      - "gamerule send_command_feedback false"
      - "gamerule command_block_output false"

    on-start:
      # Available placeholders: {player}, {game}
      # Runs once for every player after game completely starts. Runs again for individual players if they newly join.
      -

    on-death:
      # Available placeholders: {player}, {game}
      # Runs once for a single players' death
      -

    on-kill:
      # Available placeholders: {attacker}, {player}

      # Runs once for a players' death and involves the attacker.
      # [!] Beware of duplicate commands. The command actions in on-death will also run during a kill.
      - "effect give {attacker} minecraft:instant_health"
      - "show {attacker} &4+ &l2.0❤"
      - "show {attacker} -actionBar &6+ 5 coins (kill)"
      - "coins add 5 {attacker} -s"

    on-win:
      # Available placeholders {player}, {game}

      # Runs once for every player on the winning team.
      - "show {player} &6+ 10 coins ((win))"
      - "coins add 10 {player} -s"

    on-lose:
      # Available placeholders {player}, {game}

      # Runs once for every player on any losing team.
      -


  teams:
    # Team display prefixes.
    #
    # The plugin assigns teams using this list in order. Team color is automatically derived from the color codes used in each prefix.
    #
    # If this list is shorter than the number of required teams,
    # remaining teams will automatically be named: ["&aTeam 1", "&aTeam 2", "&aTeam 3"]
    names:
      ["&cTeam 1 ", "&cTeam 2 ", "&cTeam 3 ", "&cTeam 4 "]

    # Short team names used where space is limited. Should contain the same number of entries as "names".
    # These will be used to look up teams in command contexts (e.g., "/team join T1").
    short-names:
      ["T1", "T2", "T3", "T4"]

  timers:
    # Seconds players wait in the lobby before being moved into the arena.
    lobby-countdown: 10

    # Seconds players wait inside their starting cages before the game officially begins.
    game-countdown: 10

    # Seconds after the game starts before global PvP invulnerability is removed.
    invul-countdown: 15

    # Seconds after the game starts before all refillable chests are refreshed.
    chest-refill: 150

    # Seconds after the game starts before SHOWDOWN (world border event & name highlighting) begins.
    showdown: 300

    # Maximum game duration in seconds from the start until the end.
    game-end: 360


scoreboards:
  # Enables or disables scoreboards entirely
  enabled: true

  # Scoreboards do not use the CXYZ message parser, but placeholders values are still supported.
  # Minecraft scoreboards cannot contain duplicate values. You can use color codes (i.e.: "&a", "&6") for unique blank separators.

  lobby:
    - "&6&lSpeed&a&lSG" # The first entry of this list will become the scoreboard title
    - "&7{player-date-short}"
    - "&7{player-time-short}"
    - "&0"
    - "&eYou are on &f{player-team-prefix-display}"
    - "&1"
    - "&eRank: {player-rank-prefix-display}"
    - "&2"
    - "&eKills: &f{player-stat-sg-lifetime-kills} &7(#{player-stat-sg-lifetime-kills-ranking})"
    - "&eWins: &f{player-stat-sg-lifetime-wins} &7(#{player-stat-sg-lifetime-wins-ranking})"
    - "&3"
    - "&e{game-next-event}: &f{game-next-event-time}"
    - "&4"
    - "&b&l/discord"
    - "&6cerrot.xyz"

  game:
    - "&6&lSpeed&a&lSG"
    - "&7{player-date-short}"
    - "&7{player-time-short}"
    - "&1"
    - "&eYou are on &f{player-team-prefix-display}"
    - "&2"
    - "&eKills: &f{player-temp-stat-kills}"
    - "&eLives: &f{player-lives}"
    - "&eRemaining: &f{game-alive-players-size}/{game-original-players-size}"
    - "&3"
    - "&e{game-next-event}: &f{game-next-event-time}"
    - "&4"
    - "&eMap: &f{game-map}"
    - "&5"
    - "&b&l/discord"
    - "&6cerrot.xyz"

discord-leaderboards:

  top-killers:
    settings:
      stat-key: "sg-lifetime-kills"
      entry-format: "**{player}**: {player-stat-sg-lifetime-kills}"
      separator: "\n**{i}.)** "
      limit: 10

    webhook:
      webhook-url: "YOUR WEBHOOK URL HERE"

      content: ""
      embeds:
        - title: "Top Killers – SpeedSG"
          description: "{leaderboard}"
          color: 938183

          title-url: ""
          thumbnail-url: ""
          image-url: ""

          timestamp: ""

          author:
            name: ""
            url: ""
            icon-url: ""

          footer:
            text: "Last updated on {timestamp}"
            i on-url: ""

      edit-mode: true
      message-url: "https://discord.com/channels/1238973352144666737/1241447347880333342/1547888922665816097"


  top-winners:
    settings:
      stat-key: "sg-lifetime-wins"
      entry-format: "**{player}**: {player-stat-sg-lifetime-wins}"
      separator: "\n**{i}.)** "
      limit: 10

    webhook:
      webhook-url: "YOUR WEBHOOK URL HERE"

      content: ""
      embeds:
        - title: "Top Winners – SpeedSG"
          description: "{leaderboard}"
          color: 938183

          title-url: ""
          thumbnail-url: ""
          image-url: ""

          timestamp: ""

          author:
            name: ""
            url: ""
            icon-url: ""

          footer:
            text: "Last updated on {timestamp}"
            icon-url: ""

      edit-mode: true
      message-url: "https://discord.com/channels/1238973352144666737/1241447347880333342/1547888787709890573"